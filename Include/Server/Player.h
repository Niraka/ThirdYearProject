/**
A player object contains information about a player within the game world. Mostly, a Player is just used
to extend upon a standard Entity so that an Entity does not need to contain clutter.

@author Nathan */

#ifndef PLAYER_H
#define PLAYER_H

class Player
{
	private:

	protected:

	public:
		Player();
		~Player();
};

#endif